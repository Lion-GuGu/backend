package kr.ac.kumoh.likelion.gugu.point.domain;

/** 사유(비즈니스 이벤트) 코드: API/이력 응답에 노출되는 안정적 식별자 */
public enum ReasonCode {
    ANSWER_ACCEPTED,
    POST_WRITE,
    COMMENT_WRITE,
    SHOP_PURCHASE,
    ADJUST_PLUS,
    ADJUST_MINUS
}
